'use client'

import { useState } from 'react'
import Link from 'next/link'
import { ArrowUpRight, Check, Code2, GitBranch, Loader2, Sparkles, WandSparkles } from 'lucide-react'

export function WebsiteFactory() {
  const [form, setForm] = useState({ projectName: '', niche: '', pages: 'Home, About, Services, Contact', brandColors: 'Black, white, electric blue', generateSystem: true, hasLogin: false })
  const [state, setState] = useState<'idle' | 'loading' | 'done' | 'error'>('idle')
  const [result, setResult] = useState<{ projectId: string; status: string; blueprint: string } | null>(null)
  const [error, setError] = useState('')
  const update = (key: keyof typeof form, value: string | boolean) => setForm((current) => ({ ...current, [key]: value }))

  async function submit(event: React.FormEvent) {
    event.preventDefault(); setState('loading'); setError('')
    const response = await fetch('/api/website-factory/generate', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) })
    const data = await response.json()
    if (!response.ok) { setError(data.error || 'Generation failed.'); setState('error'); return }
    setResult(data); setState('done')
  }

  return <div className="space-y-8">
    <div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-muted-foreground">Creation workspace</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">Website Factory</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Turn a client brief into a launch-ready website or an entire operating system instance.</p></div><div className="flex items-center gap-2 text-xs text-muted-foreground"><span className="size-2 rounded-full bg-emerald-400" />AI Gateway connected</div></div>
    <form onSubmit={submit} className="grid gap-6 lg:grid-cols-[1.25fr_0.75fr]">
      <div className="space-y-6 rounded-xl border border-border bg-card p-6"><div className="grid gap-5 md:grid-cols-2"><label className="space-y-2 text-sm"><span className="font-medium">Project name</span><input required value={form.projectName} onChange={(e) => update('projectName', e.target.value)} placeholder="Northstar Creative" className="w-full rounded-lg border border-input bg-background px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-ring" /></label><label className="space-y-2 text-sm"><span className="font-medium">Niche</span><input required value={form.niche} onChange={(e) => update('niche', e.target.value)} placeholder="Creative agency" className="w-full rounded-lg border border-input bg-background px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-ring" /></label></div><label className="block space-y-2 text-sm"><span className="font-medium">Pages</span><input required value={form.pages} onChange={(e) => update('pages', e.target.value)} className="w-full rounded-lg border border-input bg-background px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-ring" /></label><label className="block space-y-2 text-sm"><span className="font-medium">Brand colors</span><input required value={form.brandColors} onChange={(e) => update('brandColors', e.target.value)} className="w-full rounded-lg border border-input bg-background px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-ring" /></label><div className="space-y-3 border-t border-border pt-5"><button type="button" onClick={() => update('generateSystem', !form.generateSystem)} className={`flex w-full items-center justify-between rounded-lg border p-4 text-left transition ${form.generateSystem ? 'border-primary bg-primary/10' : 'border-border'}`}><span className="flex items-center gap-3"><span className="flex size-9 items-center justify-center rounded-lg bg-primary/15 text-primary"><WandSparkles className="size-4" /></span><span><span className="block text-sm font-medium">Generate AI System Instance</span><span className="block text-xs text-muted-foreground">Build a multi-tool OS clone for this client</span></span></span><span className={`flex size-5 items-center justify-center rounded-full border ${form.generateSystem ? 'border-primary bg-primary text-primary-foreground' : 'border-muted-foreground'}`}>{form.generateSystem && <Check className="size-3" />}</span></button><button type="button" onClick={() => update('hasLogin', !form.hasLogin)} className="flex items-center gap-3 text-sm text-muted-foreground"><span className={`flex size-5 items-center justify-center rounded border ${form.hasLogin ? 'border-primary bg-primary text-primary-foreground' : 'border-input'}`}>{form.hasLogin && <Check className="size-3" />}</span>Has Login — add a Prisma User table automatically</button></div><button disabled={state === 'loading'} className="flex w-full items-center justify-center gap-2 rounded-lg bg-primary px-4 py-3 text-sm font-medium text-primary-foreground transition hover:opacity-90 disabled:opacity-60">{state === 'loading' ? <><Loader2 className="size-4 animate-spin" />Generating system blueprint...</> : <><Sparkles className="size-4" />Generate Website / System</>}</button>{error && <p className="text-sm text-destructive">{error}</p>}</div>
      <div className="space-y-4"><div className="rounded-xl border border-border bg-card p-6"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-muted-foreground">Pipeline</p><div className="mt-5 space-y-4"><PipelineItem icon={Sparkles} title="AI blueprint" active={state !== 'idle'} /><PipelineItem icon={Code2} title="Generate code" active={state === 'done'} /><PipelineItem icon={GitBranch} title="Push to GitHub" active={false} /><PipelineItem icon={ArrowUpRight} title="Deploy on Vercel" active={false} /></div></div>{result && <div className="rounded-xl border border-emerald-400/30 bg-emerald-400/5 p-6"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-emerald-400">Generation complete</p><p className="mt-2 text-sm text-muted-foreground">Saved to SQLite with status {result.status}.</p><pre className="mt-4 max-h-52 overflow-auto whitespace-pre-wrap rounded-lg bg-background p-4 text-xs leading-5 text-muted-foreground">{result.blueprint}</pre><Link href={`/website-factory/${result.projectId}`} className="mt-4 inline-flex items-center gap-2 text-sm font-medium text-primary">Open project <ArrowUpRight className="size-4" /></Link></div>}</div>
    </form>
  </div>
}
function PipelineItem({ icon: Icon, title, active }: { icon: typeof Sparkles; title: string; active: boolean }) { return <div className="flex items-center gap-3"><span className={`flex size-8 items-center justify-center rounded-lg ${active ? 'bg-primary text-primary-foreground' : 'bg-accent text-muted-foreground'}`}><Icon className="size-4" /></span><span className={`text-sm ${active ? 'text-foreground' : 'text-muted-foreground'}`}>{title}</span><span className={`ml-auto size-1.5 rounded-full ${active ? 'bg-emerald-400' : 'bg-muted-foreground/40'}`} /></div> }
