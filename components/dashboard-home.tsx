import { ArrowUpRight, Globe2, Megaphone, UserPlus } from 'lucide-react'

const stats = [{ label: 'Websites', value: '0', detail: 'No active builds', icon: Globe2 }, { label: 'Posts', value: '0', detail: 'Content queue is clear', icon: Megaphone }, { label: 'Leads', value: '0', detail: 'No leads captured', icon: UserPlus }]

export function DashboardHome() {
  return <div className="space-y-8"><section className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-muted-foreground">Monday, September 8, 2026</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">Good morning, Admin.</h2><p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">Your command center for building, automating, and operating your digital reality.</p></div><button className="inline-flex items-center gap-2 self-start rounded-md bg-primary px-4 py-2.5 text-sm font-medium text-primary-foreground hover:opacity-90">Add feature with AI <ArrowUpRight className="size-4" /></button></section><section className="grid gap-4 md:grid-cols-3">{stats.map(({ label, value, detail, icon: Icon }) => <div key={label} className="rounded-xl border border-border bg-card p-5"><div className="flex items-center justify-between"><span className="text-sm text-muted-foreground">{label}</span><Icon className="size-4 text-muted-foreground" /></div><p className="mt-6 text-4xl font-semibold tracking-tight">{value}</p><p className="mt-2 text-xs text-muted-foreground">{detail}</p></div>)}</section><section className="rounded-xl border border-border bg-card"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><h3 className="font-medium">Activity feed</h3><p className="mt-1 text-xs text-muted-foreground">Your latest system events will appear here.</p></div><span className="rounded-full border border-border px-2.5 py-1 text-[11px] text-muted-foreground">Live</span></div><div className="flex min-h-52 flex-col items-center justify-center p-8 text-center"><div className="flex size-10 items-center justify-center rounded-full bg-accent"><span className="size-2 rounded-full bg-muted-foreground" /></div><p className="mt-4 text-sm font-medium">No activity yet</p><p className="mt-1 max-w-sm text-xs leading-5 text-muted-foreground">Once you start building in TROS, your project and automation activity will be tracked here.</p></div></section></div>
}
