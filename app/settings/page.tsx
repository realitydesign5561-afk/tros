'use client'

import { useState } from 'react'
import { DashboardShell } from '@/components/dashboard-shell'
import { AuthGuard } from '@/components/auth-guard'

export default function SettingsPage() { const [message, setMessage] = useState(''); const [password, setPassword] = useState(''); const [provider, setProvider] = useState(''); const [value, setValue] = useState(''); async function savePassword(e: React.FormEvent) { e.preventDefault(); const r = await fetch('/api/settings', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ password }) }); setMessage(r.ok ? 'Password updated.' : 'Unable to update password.'); setPassword('') } async function saveKey(e: React.FormEvent) { e.preventDefault(); const r = await fetch('/api/settings', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ provider, value, label: provider }) }); setMessage(r.ok ? 'API key saved securely.' : 'Unable to save API key.'); setValue('') } return <AuthGuard><DashboardShell><div className="max-w-3xl space-y-6"><div><h2 className="text-2xl font-semibold tracking-tight">Settings</h2><p className="mt-2 text-sm text-muted-foreground">Manage your TROS access and connected services.</p></div><div className="grid gap-6 md:grid-cols-2"><form onSubmit={savePassword} className="space-y-4 rounded-xl border border-border bg-card p-5"><div><h3 className="font-medium">Admin password</h3><p className="mt-1 text-xs text-muted-foreground">Set a new password for your admin account.</p></div><input required minLength={8} type="password" placeholder="New password" value={password} onChange={e=>setPassword(e.target.value)} className="h-10 w-full rounded-md border border-input bg-background px-3 text-sm" /><button className="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground">Update password</button></form><form onSubmit={saveKey} className="space-y-4 rounded-xl border border-border bg-card p-5"><div><h3 className="font-medium">API keys</h3><p className="mt-1 text-xs text-muted-foreground">Keys are stored in your local SQLite database.</p></div><input required placeholder="Provider name" value={provider} onChange={e=>setProvider(e.target.value)} className="h-10 w-full rounded-md border border-input bg-background px-3 text-sm" /><input required type="password" placeholder="API key" value={value} onChange={e=>setValue(e.target.value)} className="h-10 w-full rounded-md border border-input bg-background px-3 text-sm" /><button className="rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground">Save API key</button></form></div>{message && <p className="text-sm text-muted-foreground">{message}</p>}</div></DashboardShell></AuthGuard> }
